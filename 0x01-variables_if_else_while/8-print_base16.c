#include <stdio.h>

/**
 * main - program that prints all the numbers of base 16 in lowercase
 *
 * return: always 0
 */
int main(void)
{
	int n;
	char letter;

	for (n = 0; n < 10; n++)
	putchar(letter);

	putchar('\n');
	return (0);
}
