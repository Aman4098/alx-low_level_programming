#include "main.h"

/**
 *print_diagonal -> print diagonal line
 * @size: integer paragme
 */
void print_diagonal(int size)
{
	int x;

	if (size <= 0)
		_putchar('\n');
	else
	{
		for (x = 0; x < n; x++)
		{
			for (i = 0; i <= x; i++)
				_putchar('_');
			_putchar(92);
			_pitchar('\n')




