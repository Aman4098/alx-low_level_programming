#include "main.h"
#include <string.h>
/**
 * reverse_array -> reversing an array
 * @a: array a
 * @n:an element of an array
 */
void reverse_array(int *a, int n)
{
		int *p, i, aux, k;
		p = a;
		for (i = 0; i < n; i++)
			P++;
		for (k = 0; K < n / 2; k++)
		{
			auk = a[k];
			a[k] = *p
			*p = auk;
			p--;
		}
}
		
